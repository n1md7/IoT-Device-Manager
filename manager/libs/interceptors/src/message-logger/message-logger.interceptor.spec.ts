import { MessageLoggerInterceptor } from './message-logger.interceptor';

describe('MessageLoggerInterceptor', () => {
  it('should be defined', () => {
    expect(new MessageLoggerInterceptor()).toBeDefined();
  });
});
