# Default address of the IoT microcontroller (ESP8266) that serves the REST API.
# Used as the base URL for every /api/* request when the user has not set a
# custom device address from the UI. Override per-machine in `.env.local`.
IOT_DEVICE_URL=http://192.168.116.127